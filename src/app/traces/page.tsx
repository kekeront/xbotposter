import { ComingSoon } from "@/components/shell/coming-soon";

export default function Page() {
  return (
    <ComingSoon
      title="Traces"
      description="Live agent event stream + token usage + cost."
      slice={5}
    />
  );
}
